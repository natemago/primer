(function($){
   /*
    * VGA, SVGA, QVGA
    */
    
    
    // Text mode...
    // http://en.wikipedia.org/wiki/VGA-compatible_text_mode
   var VGA = function(config){
      libDraw.ext(this, config);
   };
   libDraw.ext(VGA,{
        
   });
    
})(jQuery);
